# skill-recomposer

This repo uses modular TDD orchestration. Follow `src/swe/skills/swe/SKILL.md` as the tech-lead skill (in-process TDD; review subagent only after modules are built).

`architecture.md` is the source of truth for this project's architecture and recomposition targets.

To export a skill for other Cursor projects or other models, follow `.cursor/skills/recompose/SKILL.md`.
